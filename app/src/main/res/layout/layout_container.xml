<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <include
        android:id="@+id/detailInformationLayout"
        layout="@layout/detail_information_layout"
        android:visibility="gone"
        app:layout_constraintBottom_toTopOf="@id/areaCalculationsLayout"/>

    <include
        android:id="@+id/areaCalculationsLayout"
        layout="@layout/area_calculations_layout"
        android:visibility="gone"
        app:layout_constraintTop_toBottomOf="@id/detailInformationLayout"/>
</androidx.constraintlayout.widget.ConstraintLayout>