<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/add_measure_nav_graph"
    app:startDestination="@id/mapFragment">

    <fragment
        android:id="@+id/mapFragment"
        android:name="com.android.monitorporastov.fragments.MapFragment"
        android:label="fragment_map"
        tools:layout="@layout/fragment_map" >
        <action
            android:id="@+id/action_mapFragment_to_addDrawingFragment1"
            app:destination="@id/add_damage_fragment"
            app:popUpTo="@id/add_damage_fragment"
            app:popUpToInclusive="true"/>
    </fragment>
    <fragment
        android:id="@+id/add_damage_fragment"
        android:name="com.android.monitorporastov.fragments.AddOrUpdateRecordFragment"
        android:label="Informácie o poškodení"
        tools:layout="@layout/fragment_add_damage" />
</navigation>